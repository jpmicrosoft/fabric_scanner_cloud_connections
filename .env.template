# Template for environment variables
# Copy this file to .env and fill in your values

# Service Principal Authentication (Required)
# These credentials are used for scanning workspaces (reading Power BI metadata)
FABRIC_SP_TENANT_ID=your-tenant-id-here
FABRIC_SP_CLIENT_ID=your-app-client-id-here
FABRIC_SP_CLIENT_SECRET=your-app-client-secret-here

# Lakehouse Upload (Optional - for local execution only)
# When running locally, optionally upload results to Fabric Lakehouse
LAKEHOUSE_WORKSPACE_ID=your-workspace-id-here
LAKEHOUSE_ID=your-lakehouse-id-here

# Upload Authentication (Optional - for separate write permissions)
# If specified, uses separate credentials for lakehouse file uploads
# This allows principle of least privilege:
#   - Main credentials (FABRIC_SP_*) can be Viewer (read-only) for scanning
#   - Upload credentials can be Contributor (write) for file uploads
# If not specified, falls back to main credentials (requires main SPN to have Contributor role)

# Option A: Use interactive user authentication for uploads (recommended for manual runs)
# Requires: pip install msal
#UPLOAD_USE_USER_AUTH=true
#UPLOAD_TENANT_ID=your-tenant-id-here  # Optional, defaults to FABRIC_SP_TENANT_ID
#UPLOAD_CLIENT_ID=your-client-id-here  # Optional, uses default Power BI app ID if not set

# Option B: Use separate Service Principal for uploads (recommended for automation)
#UPLOAD_TENANT_ID=your-upload-tenant-id-here
#UPLOAD_CLIENT_ID=your-upload-app-client-id-here
#UPLOAD_CLIENT_SECRET=your-upload-app-client-secret-here

