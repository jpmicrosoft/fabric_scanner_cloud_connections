# Requirements for running Fabric Scanner API script locally (outside Fabric)
# Requires Python 3.8 or higher

# Core dependencies
requests>=2.31.0
pandas>=2.0.0
pyarrow>=14.0.0  # Required for parquet support in pandas

# Retry logic and resilience (production-critical)
tenacity>=8.2.0

# Optional: Progress bars (highly recommended for long-running scans)
tqdm>=4.66.0

# Optional: Configuration file support (YAML format)
# For JSON config files, PyYAML is not needed
PyYAML>=6.0.0

# Optional: Environment variable loading from .env files
python-dotenv>=1.0.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0

# Development tools
black>=23.0.0  # Code formatting
flake8>=6.1.0  # Linting
mypy>=1.7.0    # Type checking
